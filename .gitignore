venv/
__pycache__/
*.pyc
*.db
.env
instance/

# ansible/ 전체는 로컬 실행에만 필요한 산출물+실행 스크립트 사본이라 git 추적 대상이
# 아님(플레이북 원본/점검 스크립트 원본은 security-platform 레포에 있음). 로컬엔
# 그대로 두되(대시보드가 실행 시 이 경로를 읽음) 커밋만 안 함.
ansible/
scan_results/*.json
